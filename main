function mailFilterDelete() {
  const numberOfThreads = [INSERT NUMBER OF THREADS TO BE FILTERED] ;
  var tempThreads = GmailApp.getInboxThreads();
  emailFilterDelete([[INSERT EMAIL/S HERE]], numberOfThreads, tempThreads)
  senderNameFilterDelete([[SENDER NAME/S HERE]], numberOfThreads, tempThreads)
}
